// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { TrainingList } from '../types';
// import { useTrainingList } from '../hooks/useTrainingList';

// ==============================|| Training Safety PAGE ||============================== //

const TrainingListPage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useTrainingList();

  return (
    <MainCard title="Training Safety">
      <div>
        <p>Training Safety - Training List</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default TrainingListPage;
