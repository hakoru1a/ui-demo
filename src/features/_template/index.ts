// ==============================|| FEATURE_NAME PUBLIC EXPORTS ||============================== //

/**
 * Public API exports for FeatureName feature
 *
 * This file exports types, components, hooks that can be used by other features.
 * Only export what is necessary for inter-feature communication.
 */

// Export types
export type { FeatureName, FeatureNameFormData } from './types';

// Export hooks (if needed by other features)
// export { useFeatureName } from './hooks/useFeatureName';

// Export components (if needed by other features)
// export { default as FeatureNameList } from './components/FeatureNameList';

// Export API service (if needed by other features)
// export { FeatureNameService } from './api';
