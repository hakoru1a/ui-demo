// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { DriverDetail } from '../types';
// import { useDriverDetail } from '../hooks/useDriverDetail';

// ==============================|| Fleet PAGE ||============================== //

const DriverDetailPage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useDriverDetail();

  return (
    <MainCard title="Fleet">
      <div>
        <p>Fleet - Driver Detail</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default DriverDetailPage;
