// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { VehicleEdit } from '../types';
// import { useVehicleEdit } from '../hooks/useVehicleEdit';

// ==============================|| Fleet PAGE ||============================== //

const VehicleEditPage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useVehicleEdit();

  return (
    <MainCard title="Fleet">
      <div>
        <p>Fleet - Vehicle Edit</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default VehicleEditPage;
