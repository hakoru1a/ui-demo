// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { VehicleDetail } from '../types';
// import { useVehicleDetail } from '../hooks/useVehicleDetail';

// ==============================|| Fleet PAGE ||============================== //

const VehicleDetailPage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useVehicleDetail();

  return (
    <MainCard title="Fleet">
      <div>
        <p>Fleet - Vehicle Detail</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default VehicleDetailPage;
