// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { FleetList } from '../types';
// import { useFleetList } from '../hooks/useFleetList';

// ==============================|| Fleet PAGE ||============================== //

const FleetListPage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useFleetList();

  return (
    <MainCard title="Fleet">
      <div>
        <p>Fleet - List</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default FleetListPage;
