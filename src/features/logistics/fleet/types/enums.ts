// ==============================|| FLEET ENUMS ||============================== //

export type VehicleType = 'truck' | 'container'; // Loại xe: Xe tải / Container
export type VehicleStatus = 'ready' | 'running' | 'maintenance'; // Trạng thái xe
export type DriverStatus = 'available' | 'busy' | 'off' | 'dispatched'; // Trạng thái tài xế
