// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { PartnersList } from '../types';
// import { usePartnersList } from '../hooks/usePartnersList';

// ==============================|| Partners PAGE ||============================== //

const PartnersListPage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = usePartnersList();

  return (
    <MainCard title="Partners">
      <div>
        <p>Partners - List</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default PartnersListPage;
