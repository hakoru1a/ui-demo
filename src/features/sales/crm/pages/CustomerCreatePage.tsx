// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { CustomerCreate } from '../types';
// import { useCustomerCreate } from '../hooks/useCustomerCreate';

// ==============================|| CRM PAGE ||============================== //

const CustomerCreatePage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useCustomerCreate();

  return (
    <MainCard title="CRM">
      <div>
        <p>CRM - Customer Create</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default CustomerCreatePage;
