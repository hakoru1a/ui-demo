// ==============================|| ADVANCES ENUMS ||============================== //

export type AdvanceStatus = 'pending' | 'approved' | 'rejected'; // Trạng thái: Chờ duyệt / Đã duyệt / Từ chối
export type ApprovalDecision = 'approve' | 'reject'; // Quyết định: Duyệt / Từ chối
