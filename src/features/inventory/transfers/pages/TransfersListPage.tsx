// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { TransfersList } from '../types';
// import { useTransfersList } from '../hooks/useTransfersList';

// ==============================|| Transfers PAGE ||============================== //

const TransfersListPage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useTransfersList();

  return (
    <MainCard title="Transfers">
      <div>
        <p>Transfers - List</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default TransfersListPage;
