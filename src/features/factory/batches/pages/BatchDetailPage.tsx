// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { BatchDetail } from '../types';
// import { useBatchDetail } from '../hooks/useBatchDetail';

// ==============================|| Batches PAGE ||============================== //

const BatchDetailPage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useBatchDetail();

  return (
    <MainCard title="Batches">
      <div>
        <p>Batches - Detail</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default BatchDetailPage;
