// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { BatchesList } from '../types';
// import { useBatchesList } from '../hooks/useBatchesList';

// ==============================|| Batches PAGE ||============================== //

const BatchesListPage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useBatchesList();

  return (
    <MainCard title="Batches">
      <div>
        <p>Batches - List</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default BatchesListPage;
