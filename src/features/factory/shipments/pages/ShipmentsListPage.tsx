// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { ShipmentsList } from '../types';
// import { useShipmentsList } from '../hooks/useShipmentsList';

// ==============================|| Shipments PAGE ||============================== //

const ShipmentsListPage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useShipmentsList();

  return (
    <MainCard title="Shipments">
      <div>
        <p>Shipments - List</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default ShipmentsListPage;
