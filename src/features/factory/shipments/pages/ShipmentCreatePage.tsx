// project imports
import MainCard from 'components/MainCard';

// TODO: Import your types and hooks
// import { ShipmentCreate } from '../types';
// import { useShipmentCreate } from '../hooks/useShipmentCreate';

// ==============================|| Shipments PAGE ||============================== //

const ShipmentCreatePage = () => {
  // TODO: Implement your page logic
  // const { data, isLoading, error } = useShipmentCreate();

  return (
    <MainCard title="Shipments">
      <div>
        <p>Shipments - Create</p>
        <p>This is a sample page. Start building your feature here!</p>
      </div>
    </MainCard>
  );
};

export default ShipmentCreatePage;
